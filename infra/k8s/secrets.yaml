apiVersion: v1
kind: Secret
metadata:
  name: photobooth-secrets
  namespace: photobooth
type: Opaque
stringData:
  # ===== REPLACE THESE VALUES =====
  DB_CONNECTION: "Host=photobooth-db;Port=5432;Database=photobooth;Username=photobooth;Password=CHANGE_ME_DB_PASSWORD"
  DB_PASSWORD: "CHANGE_ME_DB_PASSWORD"
  SFTP_HOST: "sftp.example.com"
  SFTP_PORT: "22"
  SFTP_USERNAME: "sftp_user"
  SFTP_PASSWORD: "CHANGE_ME_SFTP_PASSWORD"
  SFTP_BASE_PATH: "/weddings"
  SFTP_PUBLIC_BASE_URL: "https://photos.example.com"
